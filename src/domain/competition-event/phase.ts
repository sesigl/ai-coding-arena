// ABOUTME: Phase enumeration for competition phases
// Defines the phases a competition can be in

export enum Phase {
  BASELINE = 'baseline',
  BUG_INJECTION = 'bug_injection',
  FIX_ATTEMPT = 'fix_attempt',
  SYSTEM = 'system',
}
